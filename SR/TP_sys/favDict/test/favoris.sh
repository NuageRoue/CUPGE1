#!/bin/bash

# 4 fonctions sont ici attendus :
#	-S : enregistre (ou ré-enregistre) un favori ; on fournit en entrée un seul et unique mot, sans espace
#	-C : change le dossier courant en le dossier associé à l'alias fourni en entrée ; il faut donc fournir un seul et unique alias en entrée, qui fasse parti de la liste;
#	-L : liste des favoris;
#	-R : retirer un favori de la liste ; 

function S ()
{
	# fonction enregistrant dans une liste un favori donné en entrée;
}
